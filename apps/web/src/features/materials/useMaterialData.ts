import {useCallback,useEffect,useState} from "react";import {domainService,materialService} from "../../domain";
export function useMaterialData(){const[data,setData]=useState<Awaited<ReturnType<typeof materialService.snapshot>>>(),[domain,setDomain]=useState<Awaited<ReturnType<typeof domainService.snapshot>>>(),[error,setError]=useState("");const refresh=useCallback(async()=>{try{const[m,d]=await Promise.all([materialService.snapshot(),domainService.snapshot()]);setData(m);setDomain(d);setError("")}catch(e){setError(e instanceof Error?e.message:"Materialdaten konnten nicht geladen werden.")}},[]);useEffect(()=>{void refresh()},[refresh]);return{data,domain,error,refresh}}
