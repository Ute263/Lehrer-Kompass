import {useCallback,useEffect,useState} from "react";
import {calendarService,domainService,lessonService} from "../../domain";
export function useCalendarData(){const[data,setData]=useState<Awaited<ReturnType<typeof calendarService.snapshot>>>(),[domain,setDomain]=useState<Awaited<ReturnType<typeof domainService.snapshot>>>(),[lessons,setLessons]=useState<Awaited<ReturnType<typeof lessonService.snapshot>>>(),[error,setError]=useState("");const refresh=useCallback(async()=>{try{await calendarService.ready();const[d,l,c]=await Promise.all([domainService.snapshot(),lessonService.snapshot(),calendarService.snapshot()]);setDomain(d);setLessons(l);setData(c);setError("")}catch(e){setError(e instanceof Error?e.message:"Kalenderdaten konnten nicht geladen werden.")}},[]);useEffect(()=>{void refresh()},[refresh]);return{data,domain,lessons,error,refresh}}
