import{useState}from"react";import{Badge,Button,Card,Notice}from"../../design-system/components";import{apiClient}from"../../server/api-client";import"./server-mode.css";
type View="choice"|"login"|"workspace"|"import";
export function ServerModePage(){const[view,setView]=useState<View>("choice"),[name,setName]=useState(""),[workspace,setWorkspace]=useState(""),[error,setError]=useState("");async function login(userId:string){setError("");try{const r=await apiClient.login(userId);setName(r.user.displayName);setWorkspace(r.workspace.name);setView("workspace")}catch(e){setError(e instanceof Error?e.message:"Anmeldung nicht verfügbar.")}}return <div className="server-mode"><header><Badge tone="info">Entwicklungsmodus</Badge><h1>Sicherer persönlicher Arbeitsplatz</h1><p>Lokale Daten und Serverdaten bleiben bewusst getrennt.</p></header>{error&&<Notice variant="error" title="Verbindung nicht verfügbar">{error}</Notice>}{view==="choice"&&<div className="mode-grid"><Card><h2>Lokaler Modus</h2><p>Weiter mit der vorhandenen Offline-App und lokalen Dexie-Daten.</p><Button onClick={()=>location.assign("/werkbank")}>Lokal weiterarbeiten</Button></Card><Card><h2>Server-Testmodus</h2><p>Mit künstlicher Identität in einen vollständig getrennten Test-Workspace wechseln.</p><Button onClick={()=>setView("login")}>Server-Testmodus öffnen</Button></Card></div>}{view==="login"&&<Card><h2>Mit Microsoft anmelden</h2><p>Persönlicher Einzelarbeitsplatz. In dieser lokalen Prüfung wird keine echte Microsoft-Anfrage gesendet.</p><div className="login-actions"><Button onClick={()=>login("personal-user-a")}>Mock: Ute Test</Button><Button variant="secondary" onClick={()=>login("personal-user-b")}>Mock: Kollegin Test</Button></div><p>Der lokale Modus bleibt ohne Anmeldung verfügbar.</p></Card>}{view==="workspace"&&<><Notice variant="success" title="Angemeldeter Workspace">{name} · {workspace}</Notice><Card><h2>Mein Arbeitsplatz</h2><p>Server-Testmodus · automatische Übertragung ist ausgeschaltet.</p><Button onClick={()=>setView("import")}>Lokale Datenübernahme prüfen</Button><Button variant="secondary" onClick={async()=>{await apiClient.logout();setView("login")}}>Abmelden</Button></Card></>}{view==="import"&&<Card><h2>Importvorschau</h2><p>Ziel: {workspace}. Lokale Daten werden erst nach einer weiteren bewussten Bestätigung übertragen und anschließend nicht gelöscht.</p><ul><li>Klassen: 2</li><li>Themen: 3</li><li>Unterrichtsstunden: 1</li><li>Materialien: 1</li></ul><Notice variant="warning" title="Prüfung erforderlich">Eine künstliche Dublette würde übersprungen.</Notice><Button onClick={()=>setView("workspace")}>Vorschau schließen</Button></Card>}</div>}
