// @vitest-environment jsdom
import{afterEach,expect,it,vi}from"vitest";import{render,screen}from"@testing-library/react";import userEvent from"@testing-library/user-event";import{ServerModePage}from"./ServerModePage";afterEach(()=>vi.unstubAllGlobals());it("lässt lokalen Modus sichtbar und meldet zwei getrennte Mock-Nutzer an",async()=>{const u=userEvent.setup();vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:true,json:async()=>({user:{displayName:"Ute Test"},workspace:{name:"Arbeitsplatz A"},csrfToken:"csrf"})}));render(<ServerModePage/>);expect(screen.getByRole("heading",{name:"Lokaler Modus"})).toBeInTheDocument();await u.click(screen.getByRole("button",{name:"Server-Testmodus öffnen"}));expect(screen.getByRole("button",{name:"Mock: Kollegin Test"})).toBeInTheDocument();await u.click(screen.getByRole("button",{name:"Mock: Ute Test"}));expect(await screen.findByText(/Ute Test · Arbeitsplatz A/)).toBeInTheDocument();expect(screen.getByText(/automatische Übertragung ist ausgeschaltet/)).toBeInTheDocument()});it("zeigt Serverfehler ruhig",async()=>{const u=userEvent.setup();vi.stubGlobal("fetch",vi.fn().mockResolvedValue({ok:false,json:async()=>({error:{code:"DATABASE_UNAVAILABLE",message:"Server nicht erreichbar",requestId:"r1"}})}));render(<ServerModePage/>);await u.click(screen.getByRole("button",{name:"Server-Testmodus öffnen"}));await u.click(screen.getByRole("button",{name:"Mock: Ute Test"}));expect(await screen.findByText("Server nicht erreichbar")).toBeInTheDocument()});
